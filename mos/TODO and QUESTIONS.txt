TODO:

- Clear / Set Escape flag should go through escapeV - not sure of the order look at riscos source
- BRKV is using direct approach, should go via handler and copy the error message to the error buffer set up when the handler was registered with OS_ChangeEnvironment



JGH/Eelco
---------

- OS_ReadLine32 - it looks like you're passing the flags in top of R1 and ignoring R3? 

	in "main_loop":
  	MOVE.L	#buffer, D0                       ; Buffer address
	MOVE.L	#$FF, D1                          ; Maximum line length is 255
	MOVE.B	#$20, D2                          ; Minimum acceptable ASCII value
	MOVE.B	#$FF, D3                          ; Maximum acceptable ASCII value
	MOVE.L	#OS_ReadLine32, A0                ; SWI OS_ReadLine32: Read line from input

	I have:
  	MOVE.L	#buffer, D0                       ; Buffer address
	MOVE.B	#$FF, D1                          ; Maximum line length is 255
	MOVE.B	#$20, D2                          ; Minimum acceptable ASCII value
	MOVE.B	#$FF, D3                          ; Maximum acceptable ASCII value
	CLR.B	D4
	MOVE.L	#OS_ReadLine32, A0                ; SWI OS_ReadLine32: Read line from input


- Break/Error/OS_GenerateError handling

	It looks like you're intercepting _errorv, rather than letting _errorv call the handler (which I read as being different in RiscOS). Also, it doesn't look like you're copying the error to the error buffer as defined in RiscOS - though I have to
	admit being midly baffled by it I think it should be something like

	...

	