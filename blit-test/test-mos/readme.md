# Test MOS

This ROM is intended to be loaded as the boot rom and will
perform a simple set of writes to the screen to test blitter
board functionality.

The aim is to not rely on RAM writes working where possible